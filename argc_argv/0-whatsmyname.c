#include <stdio.h>
/**
 * main - Prints programm's name
 * @argc: Number of arguments
 * @argv: Argument's array
 * Return: Always (0)
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
